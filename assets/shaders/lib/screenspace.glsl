// precision highp float;
// precision highp int;
// precision highp sampler2D;

// layout(binding = 1) uniform sampler2D tPathTracedImageTexture;
// layout(binding = 1) uniform sampler2D texSampler;
// layout(binding = 3) uniform sampler2D texSampler;

// void qwe()
// {	
// 	// sampler2D a;
// 	// o_Target = texelFetch(tPathTracedImageTexture, ivec2(gl_FragCoord.xy), 0);	
// } 